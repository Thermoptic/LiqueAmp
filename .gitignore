node_modules/
dist/
dev-dist/
coverage/
*.log
.vite/
e2e/output/

# environment files (see .env.example; only public values belong there anyway)
.env
.env.*
!.env.example

# Supabase CLI local state (linked project ref, versions; created by `supabase link`)
supabase/.temp/
